framework:
    messenger:
        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            sync: 'sync://'
        routing:
            App\Infrastructure\Messenger\Message\GenerateSubmissionPdfMessage: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
